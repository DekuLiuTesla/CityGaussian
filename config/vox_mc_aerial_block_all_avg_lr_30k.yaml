# Model Parameters
model_params: {
    model_config: {
        name: "GaussianModelVoxV2",
        kwargs: {
            voxel_size: [0.01, 0.01, 0.01],
            mode: "avg",
            apply_voxelize: True
        }
    },
    sh_degree: 3,
    source_path: "data/matrix_city/aerial/train/block_all",
    pretrain_path: "output/block_mc_aerial_block_all_lr_c36/point_cloud/iteration_30000",
    model_path: "",
    images: "images",
    data_device: "cuda",
    resolution: -1,
    white_background: False,
    eval: False,
    logger_config: {
        logger: "wandb",
        name: "vox_mc_aerial_block_all_avg_lr_30k",
        project: "LargeGS"
    }
}

# Pipeline Parameters
pipeline_params: {
    convert_SHs_python:  False,
    compute_cov3D_python:  False,
    pre_load: True,
    blur_level: 0,
    debug:  False
}


# Optimization Parameters
optim_params: {
    iterations: 30_000,
    position_lr_init: 0.00002,
    position_lr_final: 0.0000002,
    position_lr_delay_mult: 0.01,
    position_lr_max_steps: 30_000,
    feature_lr: 0.0025,
    opacity_lr: 0.05,
    scaling_lr: 0.005,
    rotation_lr: 0.001,
    percent_dense: 0.01,
    lambda_dssim: 0.2,
    densification_interval: 100,
    opacity_reset_interval: 3000,
    densify_from_iter: 0,
    densify_until_iter: 0,
    densify_grad_threshold: 0.0002
}


